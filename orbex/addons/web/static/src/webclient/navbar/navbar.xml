<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

  <t t-name="web.NavBar">
    <header class="o_navbar" t-ref="root">
      <t t-if="!this.ui.isSmall and !isScopedApp" t-call="web.NavBar.AppSidebar">
        <t t-set="apps" t-value="menuService.getApps()" />
      </t>
      <nav
        class="o_main_navbar d-print-none"
        data-command-category="disabled"
      >
        <div t-if="!this.ui.isSmall and !isScopedApp" class="o_orbex_topbar_brand">
          <a href="/orbex/chat" class="o_orbex_brand_link" aria-label="Orbex Suite">
            <img src="/web/static/img/logo.png" alt="Orbex Suite"/>
          </a>
          <button type="button" class="o_orbex_sidebar_toggle" t-att-title="state.isAppSidebarCollapsed ? 'Expand sidebar' : 'Collapse sidebar'" t-on-click="toggleAppSidebar">
            <i class="fa fa-bars" aria-hidden="true"/>
          </button>
        </div>
        <!-- Apps Menu -->
        <t t-call="web.NavBar.AppsMenu">
          <t t-set="apps" t-value="menuService.getApps()" />
        </t>

        <div t-if="!this.ui.isSmall and currentAppSections.length" class="o_orbex_top_sections d-print-none">
          <t t-call="web.NavBar.SectionsMenu">
            <t t-set="sections" t-value="currentAppSections" />
          </t>
        </div>

        <!-- App Brand -->
        <DropdownItem
          t-if="!this.ui.isSmall and currentApp"
          t-esc="currentApp.name"
          class="'o_menu_brand d-flex'"
          onSelected="() => this.onNavBarDropdownItemSelection(currentApp)"
          attrs="{ href: getMenuItemHref(currentApp), 'data-menu-xmlid': currentApp.xmlid, 'data-section': currentApp.id }"
        />

        <div class="o_navbar_breadcrumbs d-contents"/>

        <div t-if="!this.ui.isSmall" class="o_orbex_nav_tools d-flex align-items-center gap-2 ms-auto">
          <button type="button" class="o_orbex_nav_tool" t-att-title="state.colorScheme === 'dark' ? 'Light theme' : 'Dark theme'" t-on-click="() => this.toggleColorScheme()">
            <i class="fa fa-lightbulb-o" aria-hidden="true"/>
          </button>
          <button type="button" class="o_orbex_nav_tool" t-att-title="state.isFullscreen ? 'Exit full screen' : 'Full screen'" t-on-click="() => this.toggleFullscreen()">
            <i t-att-class="state.isFullscreen ? 'fa fa-compress' : 'fa fa-expand'" aria-hidden="true"/>
          </button>
        </div>

        <!-- Systray -->
        <div class="o_menu_systray d-flex flex-shrink-0 bg-inherit" role="menu">
          <t t-foreach="systrayItems" t-as="item" t-key="item.key">
            <!-- This ensures the correct order of the systray items -->
            <div t-att-data-index="item.index"/>
            <ErrorHandler onError="error => this.handleItemError(error, item)">
                <t t-component="item.Component" t-props="item.props"/>
            </ErrorHandler>
          </t>
        </div>
      </nav>
    </header>
  </t>

  <t t-name="web.NavBar.AppSidebar">
    <aside class="o_orbex_app_sidebar d-print-none" t-att-class="{ 'o-collapsed': state.isAppSidebarCollapsed }">
      <div class="o_orbex_sidebar_brand">
        <a href="/orbex/chat" class="o_orbex_brand_link" aria-label="Orbex Suite">
          <img src="/web/static/img/logo_sidebar.png" alt="Orbex Suite"/>
        </a>
        <button type="button" class="o_orbex_sidebar_toggle o_orbex_sidebar_toggle_collapsed" t-att-title="state.isAppSidebarCollapsed ? 'Expand sidebar' : 'Collapse sidebar'" t-on-click="toggleAppSidebar">
          <i class="fa fa-bars" aria-hidden="true"/>
        </button>
      </div>
      <nav class="o_orbex_app_list" aria-label="Modules">
        <div class="o_orbex_sidebar_section_label">
          <span>Apps</span>
          <button type="button" class="o_orbex_sidebar_toggle" t-att-title="state.isAppSidebarCollapsed ? 'Expand sidebar' : 'Collapse sidebar'" t-on-click="toggleAppSidebar">
            <i class="fa fa-bars" aria-hidden="true"/>
          </button>
        </div>
        <a
          t-foreach="apps"
          t-as="app"
          t-key="app.id"
          t-att-href="getMenuItemHref(app)"
          t-att-data-menu-xmlid="app.xmlid"
          t-att-data-section="app.id"
          class="o_orbex_app_entry"
          t-att-class="{ active: menuService.getCurrentApp() === app }"
          t-on-click.prevent="() => this.onNavBarDropdownItemSelection(app)"
        >
          <img
            class="o_orbex_app_icon"
            t-att-src="app.webIconData || '/web/static/img/default_icon_app.png'"
            t-att-alt="app.name"
          />
          <span class="o_orbex_app_name" t-esc="app.name"/>
        </a>
      </nav>
    </aside>
  </t>

  <t t-name="web.SectionMenu">
    <li class="px-3" t-if="section.childrenTree.length">
        <div class="bg-transparent"
            t-att-class="{
                'fw-bolder text-900 pt-3 pb-2': !isNested,
                'border-top': !isNested &amp;&amp; subMenu_index != 0,
                'py-2': isNested,
            }"
            t-att-data-menu-xmlid="section.xmlid" t-esc="section.name"/>
        <ul class="list-unstyled ps-0">
            <t t-foreach="section.childrenTree" t-as="subSection" t-key="subSection_index">
                <t t-call="web.SectionMenu">
                    <t t-set="section" t-value="subSection"/>
                    <t t-set="isNested" t-value="true"/>
                </t>
            </t>
        </ul>
    </li>
    <li t-else="" t-on-click="() => this._onMenuClicked(section)"  t-att-data-menu-xmlid="section.xmlid"
        t-att-class="{
            'fw-bold text-900 py-3': !isNested,
            'border-top': !isNested &amp;&amp; subMenu_index != 0,
            'py-2': isNested,
        }">
        <t t-esc="section.name"/>
    </li>
  </t>

  <t t-name="web.NavBar.AppsMenu">
    <t t-if="this.ui.isSmall">
      <a href="/orbex" class="o_menu_toggle border-0" t-att-class="{'hasImage': currentApp?.webIconData}" accesskey="h" t-ref="menuApps" t-on-click.prevent="_openAppMenuSidebar">
        <t t-call="web.NavBar.AppsMenu.Sidebar"/>
      </a>
    </t>
    <t t-else="">
      <div t-if="!isScopedApp &amp;&amp; state.isAppSidebarCollapsed" class="o_navbar_apps_menu">
        <Dropdown menuClass="'o_orbex_apps_dropdown_menu'" position="'bottom-start'">
          <button class="o_orbex_apps_toggle" data-hotkey="h" title="Apps">
            <i class="oi oi-apps" />
          </button>
          <t t-set-slot="content">
            <div class="o_orbex_launcher" t-on-click.stop="() => {}">
              <div class="o_orbex_launcher_header">
                <div class="o_orbex_launcher_tabs">
                  <span class="active">Apps</span>
                </div>
              </div>
              <div class="o_orbex_launcher_section">
                <div class="o_orbex_launcher_section_title">
                  <span>Pinned</span>
                  <button type="button" t-on-click="openMainMenuSearch">All Apps <i class="fa fa-arrow-right" aria-hidden="true"/></button>
                </div>
                <div class="o_orbex_launcher_grid">
                  <button
                    t-foreach="pinnedApps"
                    t-as="app"
                    t-key="app.id"
                    type="button"
                    class="o_orbex_launcher_app"
                    t-att-class="{ active: menuService.getCurrentApp() === app }"
                    t-att-title="app.name"
                    t-on-click="() => this.onNavBarDropdownItemSelection(app)"
                  >
                    <img t-att-src="getAppIconSrc(app)" t-att-alt="app.name"/>
                    <span t-esc="app.name"/>
                  </button>
                </div>
              </div>
              <button type="button" class="o_orbex_launcher_search" t-on-click="openMainMenuSearch">
                <span>Search...</span>
                <i class="oi oi-search" aria-hidden="true"/>
              </button>
            </div>
          </t>
        </Dropdown>
      </div>
      <div t-else="" class="mx-2"/>
    </t>
  </t>

  <t t-name="web.NavBar.AppsMenu.Sidebar">
    <i class="fa fa-bars" aria-hidden="true"/>
    <t t-portal="'body'">
      <Transition name="'o-app-menu-sidebar'" visible="state.isAppMenuSidebarOpened" leaveDuration="200" t-slot-scope="transition">
        <div class="o_app_menu_sidebar position-fixed top-0 bottom-0 end-100 d-flex flex-column flex-nowrap" t-att-class="transition.className" t-on-touchstart.stop="_onSwipeStart" t-on-touchend.stop="_onSwipeEnd">
          <div class="o_sidebar_topbar d-flex align-items-center justify-content-between flex-shrink-0 py-0 fs-4">
              <small class="o_orbex_mobile_brand d-flex align-items-center justify-content-between ms-2">
                  <a href="/orbex/chat">Orbex Suite</a>
              </small>
              <button class="o_sidebar_close oi oi-close btn d-flex align-items-center h-100 bg-transparent border-0 fs-2 text-reset" aria-label="Close menu" title="Close menu" t-on-click.stop="_closeAppMenuSidebar"/>
          </div>
          <nav class="o_burger_menu_content flex-grow-1 flex-shrink-1 overflow-auto o_burger_menu_app">
              <div class="o_orbex_mobile_section_label">Apps</div>
              <ul class="o_orbex_mobile_app_list list-unstyled py-2 ps-0 mb-0">
                <t t-foreach="apps" t-as="app" t-key="app_index">
                  <li t-att-data-menu-xmlid="app.xmlid" class="o_orbex_mobile_app" t-att-class="{'active': menuService.getCurrentApp() === app}" t-on-click="() => {this.onNavBarDropdownItemSelection(app); this._closeAppMenuSidebar();}">
                    <img t-if="app.webIconData" class="o_app_icon me-2" t-attf-src="{{app.webIconData}}"/>
                    <span t-esc="app.name"/>
                  </li>
                </t>
              </ul>
              <t t-if="currentAppSections.length">
                <div class="o_orbex_mobile_section_label"><t t-esc="currentApp.name"/> Menu</div>
                <ul class="o_orbex_mobile_sections list-unstyled py-2 ps-0 mb-0">
                  <t t-foreach="currentAppSections" t-as="subMenu" t-key="subMenu_index">
                    <t t-call="web.SectionMenu">
                        <t t-set="section" t-value="subMenu" />
                    </t>
                  </t>
                </ul>
              </t>
          </nav>
        </div>
      </Transition>
      <div t-if="state.isAppMenuSidebarOpened" class="modal-backdrop show d-block d-md-none" t-on-click.stop="_closeAppMenuSidebar" t-on-touchstart.stop="_onSwipeStart" t-on-touchend.stop="_onSwipeEnd" />
    </t>
  </t>

  <t t-name="web.NavBar.SectionsMenu">
    <div class="o_menu_sections d-flex flex-grow-1 flex-shrink-1 w-0" t-ref="appSubMenus" role="menu">
        <t t-if="!this.ui.isSmall">
            <DropdownGroup>
                <t t-foreach="sections" t-as="section" t-key="section.id">
                    <t
                        t-set="sectionsVisibleCount"
                        t-value="(sections.length - currentAppSectionsExtra.length)"
                    />

                    <t t-if="section_index lt Math.min(10, sectionsVisibleCount)">
                        <t t-set="hotkey" t-value="((section_index + 1) % 10).toString()" />
                    </t>
                    <t t-else="">
                        <t t-set="hotkey" t-value="undefined" />
                    </t>

                    <t t-if="!section.childrenTree.length">
                        <DropdownItem
                            class="'o_nav_entry'"
                            onSelected="() => this.onNavBarDropdownItemSelection(section)"
                            t-esc="section.name"
                            attrs="{
                                href: getMenuItemHref(section),
                                'data-hotkey': hotkey,
                                'data-menu-xmlid': section.xmlid,
                                'data-section': section.id,
                            }"
                        />
                    </t>
                    <t t-else="">
                        <Dropdown>
                            <button t-att-data-hotkey="hotkey" t-att-data-menu-xmlid="section.xmlid">
                                <span t-esc="section.name" t-att-data-section="section.id" />
                            </button>
                            <t t-set-slot="content">
                                <t t-call="web.NavBar.SectionsMenu.Dropdown.MenuSlot">
                                <t t-set="items" t-value="section.childrenTree" />
                                <t t-set="decalage" t-value="20" />
                                </t>
                            </t>
                        </Dropdown>
                    </t>
                </t>

                <t t-if="currentAppSectionsExtra.length" t-call="web.NavBar.SectionsMenu.MoreDropdown">
                    <t t-set="sections" t-value="currentAppSectionsExtra" />
                    <t t-if="sectionsVisibleCount lt 10">
                        <t t-set="hotkey" t-value="(sectionsVisibleCount + 1 % 10).toString()" />
                    </t>
                </t>
            </DropdownGroup>
        </t>
    </div>
  </t>

  <t t-name="web.NavBar.DropdownItem" t-inherit="web.DropdownItem" t-inherit-mode="primary">
    <xpath expr="//t[@t-tag]" position="attributes">
      <attribute name="t-att-style">props.style</attribute>
    </xpath>
  </t>

  <t t-name="web.NavBar.SectionsMenu.Dropdown.MenuSlot">
    <t t-set="style" t-value="`padding-left: ${decalage}px;`" />
    <t t-foreach="items" t-as="item" t-key="item.id">
      <DropdownItem
        t-if="!item.childrenTree.length"
        class="{
          'dropdown-item': true,
          o_dropdown_menu_group_entry: decalage gt 20
        }"
        t-esc="item.name"
        onSelected="() => this.onNavBarDropdownItemSelection(item)"
        attrs="{ style: style, href: getMenuItemHref(item), 'data-menu-xmlid': item.xmlid, 'data-section': item.id }"
      />

      <t t-else="">
        <div class="dropdown-menu_group dropdown-header" t-att-style="style" t-esc="item.name" />
        <t t-call="web.NavBar.SectionsMenu.Dropdown.MenuSlot">
          <t t-set="items" t-value="item.childrenTree" />
          <t t-set="decalage" t-value="decalage + 12" />
        </t>
      </t>
    </t>
  </t>

  <t t-name="web.NavBar.SectionsMenu.MoreDropdown">
    <div class="o_menu_sections_more">
      <Dropdown>
        <button title="More Menu" t-att-data-hotkey="hotkey">
          <i class="fa fa-plus"/>
        </button>

        <t t-set-slot="content">
          <t t-foreach="sections" t-as="section" t-key="section.id">
            <t t-if="!section.childrenTree.length">
              <DropdownItem
                class="'o_more_dropdown_section'"
                onSelected="() => this.onNavBarDropdownItemSelection(section)"
                t-esc="section.name"
                attrs="{ href: getMenuItemHref(section), 'data-menu-xmlid': section.xmlid, 'data-section': section.id }"
              />
            </t>
            <t t-else="">
              <div
                class="o_more_dropdown_section o_more_dropdown_section_group dropdown-header bg-100"
                t-esc="section.name"
              />
              <t t-call="web.NavBar.SectionsMenu.Dropdown.MenuSlot">
                <t t-set="items" t-value="section.childrenTree" />
                <t t-set="decalage" t-value="20" />
              </t>
            </t>
          </t>
        </t>
      </Dropdown>
    </div>
  </t>

</templates>
